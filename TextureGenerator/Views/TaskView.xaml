<UserControl x:Class="TextureGenerator.Views.TaskView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TextureGenerator.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>
		<ProgressBar Grid.Row="0" Height="50" Margin="10" VerticalAlignment="Bottom" Minimum="0" Maximum="100" Value="{Binding Percentage}" />
		<TextBlock Grid.Row="1" FontSize="32" HorizontalAlignment="Left" Text="{Binding Message}" />
	</Grid>
</UserControl>
